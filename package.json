{
  "name": "bloqer-v2",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "pnpm ci:prisma && turbo build",
    "dev": "turbo dev --parallel",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test --filter=@bloqer/domain --filter=@bloqer/utils --filter=@bloqer/services --filter=@bloqer/email",
    "db:generate": "dotenv -e .env -- pnpm --filter @bloqer/database exec prisma generate",
    "db:migrate": "dotenv -e .env -- pnpm --filter @bloqer/database exec prisma migrate dev",
    "db:migrate:deploy": "dotenv -e .env -- pnpm --filter @bloqer/database exec prisma migrate deploy",
    "db:migrate:status": "dotenv -e .env -- pnpm --filter @bloqer/database exec prisma migrate status",
    "db:seed": "dotenv -e .env -- pnpm --filter @bloqer/database db:seed",
    "db:seed:docs": "dotenv -e .env -- node docs/bloqer2.0/guides/capture/run-seed-docs.js",
    "ci:prisma": "pnpm --filter @bloqer/database exec prisma generate",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,css}\" --ignore-path .gitignore",
    "docs:manifest": "node docs/bloqer2.0/guides/capture/parse-captures.js",
    "docs:check-demo-env": "dotenv -e .env -- node docs/bloqer2.0/guides/capture/check-demo-env.js",
    "docs:capture-install": "pnpm --filter @bloqer/web exec playwright install chromium",
    "docs:capture-pilot": "dotenv -e .env -- node docs/bloqer2.0/guides/capture/preflight.js && dotenv -e .env -- pnpm --filter @bloqer/web exec playwright test --config ../../docs/bloqer2.0/guides/capture/playwright.config.ts pilot.spec.ts",
    "docs:capture-validate": "node docs/bloqer2.0/guides/capture/validate-screenshots.js",
    "docs:capture-validate-pilot": "node docs/bloqer2.0/guides/capture/validate-screenshots.js --pilot",
    "docs:apply-images": "node docs/bloqer2.0/guides/capture/apply-images-to-md.js",
    "docs:apply-images-pilot": "node docs/bloqer2.0/guides/capture/apply-images-to-md.js --pilot",
    "docs:capture-full": "dotenv -e .env -- node docs/bloqer2.0/guides/capture/preflight.js && dotenv -e .env -- pnpm --filter @bloqer/web exec playwright test --config ../../docs/bloqer2.0/guides/capture/playwright.config.ts full.spec.ts",
    "docs:capture-pending": "dotenv -e .env -- node docs/bloqer2.0/guides/capture/preflight.js && dotenv -e .env -- pnpm --filter @bloqer/web exec playwright test --config ../../docs/bloqer2.0/guides/capture/playwright.config.ts pending.spec.ts",
    "docs:capture-guide": "pnpm docs:manifest && pnpm docs:capture-pilot && pnpm docs:capture-full && pnpm docs:capture-pending && pnpm docs:capture-validate && pnpm docs:apply-images && pnpm docs:build-guide",
    "docs:build-guide": "node docs/bloqer2.0/guides/build_guide.js",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md,css}\" --ignore-path .gitignore"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0",
    "eslint": "^9.15.0",
    "prettier": "^3.3.0",
    "turbo": "^2.3.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
